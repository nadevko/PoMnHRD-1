<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="using:PMnHRD1.App.ViewModels"
             x:DataType="vm:Question"
             x:Class="PMnHRD1.App.Views.Question">
  <Design.DataContext>
    <vm:Question />
  </Design.DataContext>
  <StackPanel VerticalAlignment="Center" Orientation="Vertical" Margin="4">
    <TextBlock FontSize="30" TextWrapping="Wrap" TextAlignment="Center" FontWeight="SemiLight" Text="{Binding Current.Text}" />
    <!-- <ItemsControl ItemsSource="{Binding Current.Answers}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <RadioButton Content="{Binding}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl> -->
  </StackPanel>
</UserControl>
